[phases.setup]
nixPkgs = ["..."] # Esto lo llena Railway
aptPkgs = ["..."]

[variables]
    NIXPACKS_JDK_VERSION = "17"
    JAVA_VERSION = "17"

[phases.build]
    cmds = ["chmod +x gradlew", "./gradlew clean build -x check -x test -Pproduction"]
